language: cpp
compiler: gcc
dist: trusty
sudo: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-6
    - g++-6
    - cmake
    - boost

script:
  - export CXX=g++-6
  - cmake . && make
  - ./hashfun

notifications:
  email: false
