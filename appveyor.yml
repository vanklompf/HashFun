# version format
version: 1.0.{build}

# Build worker image (VM template)
image: Visual Studio 2017

configuration:
  - Debug
  - Release

before_build:
  - nuget restore

build:
  project: HashFun.sln